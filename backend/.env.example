# Backend environment template

APP_ENV=development
BACKEND_PORT=8000

# Docker / production DB
DATABASE_URL=postgresql+psycopg2://construction_user:construction_pass@db:5432/construction_db

# Local DB (uncomment if using local Postgres on host)
# DATABASE_URL=postgresql+psycopg2://construction_user:construction_pass@localhost:5432/construction_db

JWT_SECRET_KEY=change-this-secret
JWT_ALGORITHM=HS256

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

